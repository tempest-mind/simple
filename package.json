{
  "name": "simple",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -r ./dist",
    "build": "mkdir dist && cp src/index.html dist && node build.js",
    "build-full": "rm -r ./dist && mkdir dist && cp src/index.html dist && node build.js",
    "build-script": "node build.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tempest-mind/simple.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/tempest-mind/simple/issues"
  },
  "homepage": "https://github.com/tempest-mind/simple#readme",
  "dependencies": {
    "axios": "^1.1.3",
    "dotenv": "^16.0.3",
    "fs": "0.0.1-security",
    "global-agent": "^3.0.0",
    "https-proxy-agent": "^5.0.1",
    "path": "^0.12.7"
  }
}
